Code style: ES2022 modules with .js extensions, 2-space indents, single quotes, semicolons. Follow ESLint + Prettier. UI uses vanilla HTML/CSS with BEM-ish classes, data-i18n attributes for localization, no inline scripts/eval due to MV3 CSP. Providers extend BaseProvider implementing validateConfig/translate/getModels. Storage via chrome.storage.local, messaging via chrome.runtime.sendMessage. Maintain consistent design tokens, sanitize inputs, minimize permissions, avoid logging API keys.